name: mobile-react-app::hardened
on:
  workflow_run:
    workflows: ["mobile-react-app::integrated"]   # must match the upstream name: exactly
    types: [completed]
jobs:
  harden:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}   # don't proceed past a failure
    steps:
      - uses: actions/checkout@v4
      - run: echo "audit / secrets / sast…"
