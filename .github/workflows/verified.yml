name: mobile-react-app::verified
on:
  workflow_run:
    workflows: ["mobile-react-app::hardened"]
    types: [completed]
jobs:
  verify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4
      - run: echo "deploy…"
